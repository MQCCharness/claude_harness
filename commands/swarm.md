# 蜂群模式（4+1 · L2）— Claude Code 适配版

请在本仓库按 **4+1 蜂群模式** 处理本条及后续追问（除非我另行写明退出或豁免口令）。

## 必须遵守的权威源

- `CLAUDE.md §二`（编队时序闸门、写码回合 **R3+R4a+R4b**、R2 强制触发、R4 清单实测与 PASS/FAIL、豁免口令、**统帅白名单**含 **`.claude/agents/runs/`** 链级固化等）
- 与军团叠加时：`CLAUDE.md §三` 与之 **并集取严**（见 `AGENTS.md`）

## 角色固化（派发 Agent 前必读 · 稳定 ROLE_DEF 与 Agent 触发）

- **`AGENTS.md`**：固化目的、**(α)(β)(γ)** 顺序、与 **Agent 子代理**关系（盘稿**不替代** Agent）。
- **`.claude/agents/README.md`**：盘稿首要目的、固化时机、主窗口与 Agent 边界、主帅收口检查清单。
- **`CLAUDE.md §六`**：**Agent 子代理使用规范**（**角色 MD 真源**、固化时机 (α)(β)(γ)、**`## 本链绑定`** / **`### 最近派发 Task 摘要`**、**`ROLE_DEF=`**、禁止零差异化模板副本）。
- **链上盘稿与脚手架**：
  - 生产链：`META.md` + `swarm-r1.md` … `swarm-r4b.md` 置于 **`.claude/agents/runs/<CHAIN-ID>/`**
  - Fork 起点（勿当生产 CHAIN 改写）：**`.claude/agents/runs/_TEMPLATE-SWARM-B4P1/`**（五段模板，已含绑定与 Task 摘要占位节）

## 起手与路由

- 命中强触发时按 `CLAUDE.md §二` 完成最小 Read 集；总路由速查：`.claude/skills/hajimi-dog/00_全局索引/SKILL.md`
- 按需下钻技能 **09 / 12 / 16 / 22** 等。

## 执行纪律（摘要）

- **统帅（+1）** 驻主对话：解析 → 分发 → 汇流 → 续聊任务状态；**R1/R2/R3/R4a/R4b** 在强制情形下须 **Agent 子代理**真隔离，禁止主对话分饰多角代替。
- **写码回合**：**R1+R2 关门 → R3**；**R4a** 可与 R3 并行（契约级、不喂实现细节）；**R3 关门且证据回灌后 → R4b**；统帅按 R4a 表 **亲自实测** 再裁决 PASS/FAIL。
- 若我本轮需要 **主帅直接改仓**，我会另写 **「统帅可落地代码」** 或 **「主帅直接改仓」**；未写则默认 **统帅实现轨外置**（R3 Agent 落笔）。

## 我的本轮诉求

（在此继续写具体任务、约束、CHAIN-ID、验收口径等。）
